'use client';

// EditorBottomPanel is no longer used in the desktop layout.
// Tools are now opened as individual overlay modals/drawers (v1 behavior).
// This file is kept as a stub to avoid import errors during transition.

export function EditorBottomPanel() {
  return null;
}
